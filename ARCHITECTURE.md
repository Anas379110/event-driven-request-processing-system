# 🏗️ المعمارية — نظام معالجة الطلبات المبني على الأحداث

## نظرة عامة على المعمارية
معالجة آلاف الأحداث في الثانية عبر خدمات متعددة. المعمارية مبنية بشكل يضمن قابلية التوسع (Scalability) والصيانة (Maintainability) على المدى الطويل.

## المكوّنات الرئيسية
- **طبقة الاستقبال (API Gateway):** نقطة دخول موحّدة لكل الطلبات، تتولى التوجيه، تحديد المعدل (Rate Limiting)، والمصادقة الأولية.
- **طبقة الخدمات (Services):** خدمات مستقلة، كل واحدة مسؤولة عن نطاق عمل محدد ولها منطقها ومخطط بياناتها الخاص.
- **طبقة الاتصال غير المتزامن:** Apache Kafka لتمرير الأحداث بين الخدمات دون اقتران مباشر (Loose Coupling).
- **طبقة البيانات:** قاعدة بيانات مخصصة لكل خدمة أو مجموعة خدمات مترابطة، مع طبقة تخزين مؤقت أمامها لتقليل الحمل.
- **طبقة المراقبة:** تجميع السجلات (Logs) والمقاييس (Metrics) من كل خدمة في مكان مركزي لتتبع الأداء والأعطال.

## تدفّق البيانات (Data Flow)
الطلب يدخل عبر API Gateway → يُوجَّه إلى الخدمة المعنية → الخدمة تعالج المنطق وتتفاعل مع قاعدة بياناتها → أي حدث ناتج يُنشر على طابور الرسائل لتستهلكه الخدمات المهتمة بشكل غير متزامن.

## قابلية التوسّع (Scalability)
النظام مصمم للتوسّع الأفقي (Horizontal Scaling) — يمكن تشغيل عدة نسخ من كل مكوّن خلف موازن حمولة (Load Balancer) دون تغيير في منطق العمل، وتمت مراعاة ذلك منذ مرحلة التصميم الأولى بدل تركه لمرحلة لاحقة.

## المرونة تجاه الأعطال (Resilience)
تم اعتماد مبدأ الفشل الآمن (Fail Gracefully): مهلات زمنية (Timeouts) محددة لكل استدعاء خارجي، إعادة محاولة (Retry) محدودة مع تأخير تصاعدي (Exponential Backoff)، وحدود قاطعة (Circuit Breaker) لمنع انهيار متسلسل بين الخدمات المترابطة.

---
*جزء من [توثيق المشروع الكامل](./PROJECT.md)*
