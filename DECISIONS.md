# 📋 القرارات المعمارية (ADR) — نظام معالجة الطلبات المبني على الأحداث

سجل موجز لأهم القرارات التقنية التي تم اتخاذها في هذا المشروع، مع السياق والسبب والمقايضة (Trade-off) لكل قرار.

## القرار 01: اعتماد بنية الخدمات المصغّرة (Microservices) بدل النظام الأحادي (Monolith)

**السياق:** النظام يضم عدة نطاقات عمل مستقلة (وحدات وظيفية متعددة) بمعدلات نمو وتحميل مختلفة لكل وحدة.

**القرار:** تم تقسيم النظام إلى خدمات مستقلة، كل خدمة مسؤولة عن نطاق عمل واحد ولها قاعدة بياناتها الخاصة أو مخطط بيانات منفصل.

**السبب:** يسمح هذا بنشر كل خدمة وتوسيعها (Scale) بشكل مستقل دون التأثير على باقي النظام، ويقلل من أثر الأعطال (Blast Radius).

**المقايضة (Trade-off):** زيادة تعقيد التنسيق بين الخدمات (Service Discovery, Distributed Tracing)، تمت معالجتها عبر API Gateway وأدوات مراقبة موزعة.

---

## القرار 02: استخدام Apache Kafka كناقل أحداث بدل قوائم الرسائل التقليدية

**السياق:** الحاجة لمعالجة أحداث بمعدلات مرتفعة مع ضمان ترتيب الأحداث والقدرة على إعادة معالجتها (Replay).

**القرار:** تم اعتماد Kafka كـ Event Bus مركزي بين الخدمات مع تقسيم الأحداث حسب النطاق (Topics) و Partitioning حسب معرّف الكيان.

**السبب:** Kafka يوفر إمكانية الاحتفاظ بسجل الأحداث (Event Log) وإعادة تشغيله، وهو ما لا توفره قوائم الرسائل التقليدية بنفس الكفاءة.

**المقايضة (Trade-off):** منحنى تعلّم أعلى وإدارة أثقل للـ Brokers مقارنة بـ RabbitMQ، تم قبوله مقابل ضمانات الاتساق والاسترجاع.

---

## القرار 03: تحزيم كل الخدمات عبر Docker

**السياق:** الحاجة لبيئة تشغيل متطابقة بين التطوير والاختبار والإنتاج، وتسهيل النشر على بنى تحتية مختلفة.

**القرار:** تم تحزيم كل مكوّن من مكوّنات النظام كـ Docker Image منفصل مع Docker Compose للتطوير المحلي.

**السبب:** يلغي هذا مشكلة 'يعمل عندي بس' ويسهّل النشر على أي بيئة تدعم الحاويات (سحابية أو محلية).

**المقايضة (Trade-off):** تكلفة تعلّم إضافية بسيطة للفريق غير المعتاد على الحاويات، عولجت عبر توثيق داخلي مختصر لأوامر التشغيل الشائعة.

---

## القرار 04: بناء خط CI/CD آلي

**السياق:** النشر اليدوي المتكرر كان يستهلك وقتاً وعرضة لأخطاء بشرية عند كل تحديث.

**القرار:** تم إعداد خط أنابيب آلي (بناء → اختبار → نشر) يعمل تلقائياً عند كل Push على الفرع الرئيسي.

**السبب:** يقلل هذا زمن الوصول للتغييرات إلى الإنتاج (Lead Time) ويضمن تشغيل نفس مجموعة الاختبارات قبل كل نشر.

**المقايضة (Trade-off):** حاجة لضبط بيئة الاختبار الآلي بعناية لتفادي False Positives، تم التعامل معها بتحسين الاختبارات تدريجياً.

---

*جزء من [توثيق المشروع الكامل](./PROJECT.md)*
